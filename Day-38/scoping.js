let a=12;
//?Can be accessed from anywhere so it is global scoped


function a(){
    let b=45;
    //?Function scoped : you can only use this b variable inside the function only but can't be accessed from outer side
}

//*-------------------------------------------------------------------------------------------------------*//